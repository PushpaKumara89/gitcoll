name: Print Hello World

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "hello world"